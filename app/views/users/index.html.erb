<h1>Users</h1>

<ul>
<% if current_user %>
  <% @users.each do |user| %>
    <li>
    test
      <%= user.name %>
      <%= link_to 'User information', user_path(user) %>
      # <% byebug %>
    </li>
  <% end %>
  <% end %>
</ul>

